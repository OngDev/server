export interface YoutubeStats {
  viewCount: number;
  subscriberCount: number;
  videoCount: number;
}

export interface GithubStats {
  repoCount: number;
  gistCount: number;
  followerCount: number;
}
